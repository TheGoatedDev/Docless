# Auto-rename with a small local text model

**Status:** deprecated — feature removed. Sidecar column `auto_renamed` (schema v3) left in place; forward-only migrates.

Was: after OCR, queue `llama3.2:1b` DATE/VENDOR/WHAT and `fs.rename` to `YYYY-MM-DD-Vendor-What.ext`.
