# Tray dual window

The app is tray-resident: a full main window plus a compact tray window (`--docless-window=compact`). Closing windows does not quit the process; quit is tray menu or Cmd+Q. Compact is for quick access; main is the full UI.
