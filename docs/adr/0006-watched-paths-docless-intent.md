# Watched paths and `.docless` intent

Users will add watched filesystem paths. For each watched root, Docless will keep app metadata under a `.docless` directory in that root so data stays next to the files (local-first, portable with the folder).

Watcher: ADR 0007. On-disk layout / DB: ADR 0008.
